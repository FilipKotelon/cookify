@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<div class="row">
    <div class="col-6">
        <h2 class="text-primary">List of images:</h2>
    </div>
</div>

<br />
<div class="p-4 border rounded">
    @if (Model.Count > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Id
                    </th>
                    <th>
                        Name
                    </th>
                    <th>
                        Description
                    </th>
                    <th>
                        Accepted
                    </th>
                    <th>
                        Category
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var recipe in Model)
                {
                <tr>
                    <td width="10%">@recipe.Id</td>
                    <td width="20%">@recipe.Name</td>
                    <td width="30%">@recipe.Description</td>
                    <td width="10%"><input type="checkbox" checked="@recipe.Accepted" /></td>
                    <td width="30%">@recipe.RecipeCategory.Title</td>
                </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>There are no images in the database!</p>
    }
</div>