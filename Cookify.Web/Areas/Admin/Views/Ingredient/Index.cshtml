<div class="admin-header">
    <h1 class="admin-header__title">Składniki</h1>
    <a asp-action="Upsert" class="admin-btn">Dodaj nowy</a>
</div>

@if (Model.Count > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th width="10%">
                    Id
                </th>
                <th width="30%">
                    Nazwa
                </th>
                <th width="40%">
                    Opis
                </th>
                <th width="10%">
                    Edytuj
                </th>
                <th width="10%">
                    Usuń
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var ingredient in Model)
            {
            <tr>
                <td width="10%">@ingredient.Id</td>
                <td width="30%">@ingredient.Name</td>
                <td width="40%">@ingredient.Description</td>
                <td width="10%"><a asp-action="Upsert" asp-route-id="@ingredient.Id" class="admin-btn">Edytuj</a></td>
                <td width="10%"><a asp-action="Delete" asp-route-id="@ingredient.Id" class="admin-btn">Usuń</a></td>
            </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>Nie znaleziono składników w bazie danych!</p>
}