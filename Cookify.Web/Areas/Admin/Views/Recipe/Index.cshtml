<div class="admin-header">
    <h1 class="admin-header__title">Przepisy</h1>
    <a asp-action="Upsert" class="admin-btn">Dodaj nowy</a>
</div>

@if (Model.Count > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th width="10%">
                    Id
                </th>
                <th width="20%">
                    Name
                </th>
                <th width="30%">
                    Description
                </th>
                <th width="10%">
                    Accepted
                </th>
                <th width="10%">
                    Category
                </th>
                <th width="10%">
                    Edit
                </th>
                <th width="10%">
                    Delete
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var recipe in Model)
            {
                <tr>
                    <td width="10%">@recipe.Id</td>
                    <td width="20%">@recipe.Name</td>
                    <td width="30%">@recipe.Description</td>
                    <td width="10%"><input type="checkbox" disabled checked="@recipe.Accepted" /></td>
                    <td width="10%">@recipe.RecipeCategory.Title</td>
                    <td width="10%"><a asp-action="Upsert" asp-route-id="@recipe.Id" class="admin-btn">Edit</a></td>
                    <td width="10%"><a asp-action="Delete" asp-route-id="@recipe.Id" class="admin-btn">Delete</a></td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>There are no recipes in the database!</p>
}