@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<div class="row">
    <div class="col-6">
        <h2 class="text-primary">Lista kategorii przepisów:</h2>
    </div>
    <div class="col-6 text-right">
        <a class="btn btn-primary text-light" asp-action="Upsert">Utwórz nową kategorię przepisu</a>
    </div>
</div>

<br />
<div class="p-4 border rounded">
    @if (Model.Count > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Tytuł
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var recipeCategory in Model)
                {
                <tr>
                    <td width="70%">@recipeCategory.Title</td>
                </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>Nie znaleziono przepisów w bazie danych!</p>
    }
</div>